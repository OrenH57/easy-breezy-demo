services:
  - type: web
    name: easy-breezy-demo
    runtime: node
    buildCommand: npm ci && npm run build
    startCommand: npm start
    envVars:
      - key: ADMIN_PASSWORD
        generateValue: true
      - key: TRUST_PROXY
        value: true
